import Card from "./Card";


function Top({cards}) {


    return(
        <div className='top'>

            {/* {cards.map((card) => (
				<Card isActive={isActive ? : null}
                active={card.active}
                cardnumber={card.cardnumber} 												
				/>
			))} */}
            

        </div>
    )
}

export default Top;